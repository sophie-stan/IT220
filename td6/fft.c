#include <float.h>
#include <stdlib.h>
#include <math.h>

#include "fft.h"


unsigned short 
*backward(int normalisation, int rows, int cols, fftw_complex* freq_repr)
{
  (void) normalisation;
  (void) rows;
  (void) cols;
  (void) freq_repr;
  return NULL;
}

